export default function PageLoader() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-bg">
      <span className="font-mono text-xs uppercase tracking-[0.3em] text-muted animate-pulse">
        Loading\u2026
      </span>
    </div>
  );
}